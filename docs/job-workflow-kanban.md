# Pixel Kanban Job Workflow

Adds a Jobs button concept for opening a pixel Kanban dialog that maps existing workflow steps into Backlog, Doing, Review, and Done columns.

The desired default button position is stored in `setting-config.json` as `defaults.sceneButtonFrames.jobWorkflow` so it aligns with the existing scene controls.
